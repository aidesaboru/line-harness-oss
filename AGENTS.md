# ec-owner-line-harness

## 親ルール

`/Users/shinichi/Project/AGENTS.md` を継承する。
このファイルには、このプロジェクトだけの差分を書く。

共通ルールの正本は `/Users/shinichi/Project/.codex/rules/`。

## 目的

LINE公式アカウントCRMとマーケティング自動化の検証ハーネス。

## 主要コマンド

- `corepack pnpm --filter worker dev`
- `corepack pnpm --filter web dev`
- `corepack pnpm -r build`
- `vitest run --config vitest.config.ts`

## 検証

- `vitest run --config vitest.config.ts`
- `corepack pnpm -r build`

## 固有注意

- ゴールから外れる提案をしない
- ゴールに進む提案を必ずする
- 回答では「次のタスク」と「全体像から見た進捗」を分かる言葉で整理する
- 大学生にも分かる粒度で説明する

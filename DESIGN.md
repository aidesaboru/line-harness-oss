status: active

# Lリンク Design Contract

## 対象と目的

- 対象: Lリンクの運営スタッフ、営業責任者、アポインター
- 主要操作: 営業連絡前にAIで生成された会話内容の要約と直近1〜3か月の活動量を確認し、運営側が個別チャットから営業ステータスと判断メモを手動更新する
- 与えたい印象: 緊急度がすぐ分かり、安心して仕事に使える実務ツール
- 残す製品らしさ: 白地、明るいグレーの境界線、LINEグリーン、日本語業務UIの低密度な構成

## 参照

- 主参照: Lリンクの既存管理画面、参照日 2026-09-09
  - 残す特徴: `#06C755` の操作色、白い面、8〜12pxの角丸、小さな文字スケール、固定サイドバー
- 補助参照: [Lazyweb customer-status research](https://www.lazyweb.com/agentic-search/59b2d7f6-0430-42c2-801d-c886c36c8252)、参照日 2026-09-09
  - Baremetrics: 顧客状況の一覧・検索・絞り込み
  - UserGems: リスクの高い顧客を先に見せる並び
  - Jobber: 集計と顧客一覧の同一画面配置
  - Zoho: 詳細内の共有メモと時系列履歴
  - Solidroad / Front: 会話画面の右側に、顧客情報と手動ステータスをまとめる
  - Freshdesk: 状況・最近の活動・履歴を同じ詳細面で追える構成
- 採用しない表現: ダークモード、グラデーション、装飾的なダッシュボード図表、大きなLP風ヒーロー、会話本文の再掲

## トークン

- 色: brand `#06C755`、text `gray-950/700/500`、border `gray-200/300`、surface `white/gray-50`
- 状況色: 通常 `emerald`、要注意 `amber`、クレーム `red`、退会手続き `rose`、確認前・退会済み `slate/gray`
- 書体: Noto Sans JP を先頭にした既存システムフォント
- 幅: 一覧は可変、詳細は desktop 340〜420px
- 余白: 8px基準、ページ 16〜32px、行 16px
- 角丸: control 8px、panel 12px、badge 999px
- 境界線と影: 1pxの明るい境界線が主。影は面の分離に必要な小さな `shadow-sm` まで

## コンポーネントと状態

- 集計ボタン: default=淡い状況色、active=黒リング、hover=境界線強調、focus=green ring
- 絞り込み: default=白、active=黒地白文字、横スクロール可
- 顧客行: default=白、hover=gray-50、active=green-50、キーボードfocusあり
- 詳細: desktop=右カラム、mobile=全画面シート。営業アクション、最近の状況、1〜3か月の匿名化集計、ステータス判断メモ、更新、履歴の順
- 匿名化集計: 会話本文・送信者・メッセージIDは営業APIへ出さない。顧客発言数、担当者返信数、自動配信数、稼働日、画像・添付数、最終接触、未返信傾向、関連チケット数だけを表示する
- 最近の状況: 直近の人同士のテキストを最大80件に絞り、氏名・連絡先・URL・金額・金融機関・外部ID・認証情報をWorker内で伏せてからCloudflare Workers AIへ渡す。相談内容、これまでの対応、現在の状況、次の対応を4つの日本語文として受け、数字・コード・M番号だけの項目や、日時と会話があった事実だけの汎用文を品質検証で拒否し、出力をもう一度伏せた要約だけをv2領域へ保存する。品質検証を2回とも通らない場合は曖昧なAI文を保存せず、具体的内容を確認できなかったことと元チャットの人手確認が必要なことを固定文で記録する。要約基準日時を渡し、最終記録後の進展は推測せず、古い記録はその後を確認できないと明記する。v1記録は監査用に保持するが画面とAPIには表示しない。原文・プロンプトは保存・ログ出力せず、dry-runで対象件数と推定入力トークンを確認してから一括生成する
- 個別チャット: 顧客詳細の上部に最近の状況と営業ステータスを分けて置き、会話を確認した運営担当者がステータスと判断メモを手動で変更する
- 更新フォーム: 確認前は状況を未選択のまま開始する。人が状況を選ぶまで保存不可。未変更・空の判断メモ・保存中も disabled、競合時は最新状況を再取得
- プライバシー: ステータス判断メモに会話本文や個人情報を転記しない。電話番号・メール・URLの明らかな形式は保存時にも伏せる。AI会話要約はステータス判定へ利用せず、ステータスの自動判定・自動更新は行わない
- loading=行と詳細のスケルトン、error=再読み込み、empty=条件変更の案内

## アイコン・モーション・アクセシビリティ

- アイコンは既存の線画SVGに統一し、装飾画像は使わない
- 動きは hover/focus/モバイル詳細の開閉に限定する
- タップ対象は原則44px以上、色だけでなく文字ラベルで状況を示す
- 検索、絞り込み、顧客行、保存はキーボード操作と可視focusを持つ

## レスポンシブと検証

- desktop: 1440×1000、一覧と詳細の同時表示
- mobile: 390×844、集計は2列、状況チップは横スクロール、詳細は全画面
- 検証対象: 閲覧専用・運営編集の両権限、確認前の手動選択、1〜3か月切替、本文非公開、連絡先伏字、未返信傾向、空状態、保存、履歴、競合、アカウント切替
- validated_scope: 2026-09-10に一時ローカルD1・架空顧客のみで検証済み。desktopでメニュー順、運営の顧客状況詳細、1〜3か月集計、個別チャット右側の手動更新、営業閲覧専用の単一メニューと直URL制限を確認。D1互換SQLで本文非公開、集計値、更新拒否、連絡先伏字を確認。mobileは既存の全画面詳細構造とビルドを確認し、実寸スクリーンショットは未確認
